package edu.nyu.sz1288;

import weka.core.Instances;
import weka.filters.Filter;
import weka.filters.unsupervised.attribute.Discretize;
import weka.filters.unsupervised.attribute.Remove;

public class Filtering {
	public static Instances applyFilter(Instances data, String[] options) throws Exception {
		Remove remove = new Remove();
		remove.setOptions(options);
		remove.setInputFormat(data);
		data = Filter.useFilter(data, remove);
		return data;
	}

	public static Instances descretize(Instances data, int column) {
		Discretize filter = new Discretize(String.valueOf(column));
		filter.setBins(2);
		filter.setMakeBinary(true);
		filter.useFilter(data, filter);
	}
}
